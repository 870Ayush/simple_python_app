[pytest]
addopts = -v --strict-markers
markers =
    group1: home ("/") route tests
    group2: health probe ("/healthz") tests
    group3: APP_VERSION environment handling tests
    group4: routing, HTTP method and error handling tests
